# SwiftUI-Canvas — Agent 入口

本项目文档待补充；工作区公共约定见 `/Users/iason/Projects/AGENTS.md`。

## 参考项目仓库（_references）

可以把 `_references`（`/Users/iason/Projects/_references/`）当作本机统一存放参考项目仓库的地方。需要参考其他项目时先到这里查找；新增的参考仓库也克隆到这里，不要放进本项目内部。确需把参考代码复制或 fork 进本项目时，先在本文件中说明原因。

## 权限

操作遇到权限不足时，直接向用户说明需要的权限并申请，不要静默绕过或放弃任务。
